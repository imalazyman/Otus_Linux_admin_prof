/var/log/borg-backup/backup.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 644 vagrant vagrant
    postrotate
        systemctl reload borg-backup.service > /dev/null 2>&1 || true
    endscript
}